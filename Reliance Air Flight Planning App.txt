<!DOCTYPE html><html lang="en"><head>  <meta charset="utf-8" />  <meta name="viewport" content="width=device-width,initial-scale=1" />  <title>Reliance Air Charters Flight Planner</title>  <style>    :root{      --max-width: 1000px;      --row-gap: 10px;      --grey-bg: #f2f2f2;      --field-height: 44px;      --field-font-size: 16px; /* Base font size */    }    html,body{height:100%;margin:0;padding:0;font-family:Arial, sans-serif;background:#fff}    .container{      width:100%;      max-width:var(--max-width);      margin:0 auto;      padding:20px;      box-sizing:border-box;    }    .header { text-align:left; margin-bottom:20px; color:#333 }    .title { font-size:28px; font-weight:700 }    .subtitle { font-size:18px; }    .controls{      display:flex;      gap:10px;      margin-bottom:var(--row-gap);      width:100%;      box-sizing:border-box;    }    .jet-select, .speed-select{      padding:8px;      font-size:calc(var(--field-font-size) * 1.15); /* Increased by 15% */      background-color:var(--grey-bg);      border:1px solid #bbb;      border-radius:4px;      width:50%;      box-shadow:none;      box-sizing:border-box;      height:var(--field-height);    }    table{      width:100.5%;      border-collapse:collapse;      margin-bottom:var(--row-gap);      table-layout:fixed;      box-sizing:border-box;      margin-left:-4px; /* shift table 4px left as requested */    }    /* columns: departure 31%, arrival 31%, distance 22%, time 16% */    colgroup col:nth-child(1){ width:31% }    colgroup col:nth-child(2){ width:31% }    colgroup col:nth-child(3){ width:22% }    colgroup col:nth-child(4){ width:16% }    th{      padding:6px 6px;      text-align:left;      color:#333;      background:#fff;      font-weight:600;      font-size:calc(var(--field-font-size) * 1.15 * 1.1); /* Increased by 10% from previous 15% */    }    td{      padding:4px 4px;      vertical-align:middle;      position:relative;    }    input[type="text"]{      height: var(--field-height);      min-height: var(--field-height);      box-sizing: border-box;      padding: 8px;      border-radius: 4px;      border: 1px solid #ccc;      background: #fff;      width:100%;      overflow:hidden;      text-overflow:ellipsis;      white-space:nowrap;      font-size:calc(var(--field-font-size) * 1.1); /* Increased by 10% */      line-height: normal;      display:block;    }    /* Distance/time are inputs too but readonly; styled identical */    input[readonly] {      background: #fff;      color: #000;      cursor: default;    }    input:focus, .jet-select:focus, .speed-select:focus {      outline: none;      box-shadow: none;      border-color: #999;    }    /* suggestions dropdown styling */    .autocomplete-list {      position: absolute;      left: 0;      top: calc(var(--field-height) + 6px);      background: var(--grey-bg);      border: 1px solid #bbb;      border-radius: 4px;      max-height: 220px;      overflow-y: auto;      z-index: 9999;      display: none;      box-sizing:border-box;      width: 100%;      padding: 4px 0;    }    .autocomplete-item {      padding: 8px 10px;      cursor: pointer;      font-size: 14px;      color: #000;      white-space: nowrap;    }    .autocomplete-item:hover { background:#e6e6e6; }    .buttons { margin:0 0 var(--row-gap) 0; width:100%; box-sizing:border-box; }    button {      width:100%;      padding:10px;      background:var(--grey-bg);      color:#000;      border:1px solid #666;      cursor:pointer;      font-size:calc(var(--field-font-size) * 1.15); /* Increased by 15% */      border-radius:4px;      margin-bottom:var(--row-gap);      box-shadow:none;      box-sizing:border-box;      height:var(--field-height);    }    button:hover{ background:#e0e0e0; }    #map{      height:800px;      width:100%;      border:1px solid #ddd;      border-radius:4px;      margin-top:calc(var(--row-gap) - 17px); /* Move map down by 8px from previous -25px */      box-sizing:border-box;    }    #status{      margin:0 0 var(--row-gap) 0;      padding:10px;      background:#fff3cd;      border:1px solid #ffeaa7;      border-radius:4px;      display:none;      box-sizing:border-box;    }    .calculation-result{ margin-top:var(--row-gap); padding:15px; background:#fff; border:1px solid #ddd; border-radius:4px; box-sizing:border-box; }    .loader{ display:none; text-align:center; padding:20px; background:#f8f9fa; border:1px solid #ddd; margin-top:var(--row-gap); border-radius:4px; box-sizing:border-box; }    .loader.active{ display:block; }    @media (max-width:520px){      .controls{ flex-direction:column }      .jet-select,.speed-select{ width:100% }      colgroup col:nth-child(1), colgroup col:nth-child(2), colgroup col:nth-child(3), colgroup col:nth-child(4) { width:auto }      .autocomplete-list { max-height:200px }    }  </style></head><body>  <div class="container">    <div class="header">      <div class="title">RELIANCE AIR CHARTERS ¨</div>      <div>GLOBAL AIRPORTS FLIGHT ROUTE PLANNER APP</div>    </div>    <div class="controls">      <select id="jetSelect" class="jet-select">        <option>BEECHCRAFT KING AIR B200</option>        <option>BEECHCRAFT KING AIR B350</option>        <option>BEECHCRAFT KING AIR 350I</option>        <option>BOEING 747-400 FREIGHTER</option>        <option>BOMBARDIER CHALLENGER 300</option>        <option>BOMBARDIER CHALLENGER 350</option>        <option>BOMBARDIER CHALLENGER 601 3AER</option>        <option>BOMBARDIER CHALLENGER 604</option>        <option>BOMBARDIER CHALLENGER 650</option>        <option>BOMBARDIER CHALLENGER 850ER</option>        <option>BOMBARDIER CRJ 200ER</option>        <option>BOMBARDIER CRJ 900ER</option>        <option>BOMBARDIER CRJ 1000ER</option>        <option>BOMBARDIER DASH 8 Q200</option>        <option>BOMBARDIER DASH 8 Q300</option>        <option>BOMBARDIER DASH 8 Q400</option>        <option>BOMBARDIER GLOBAL EXPRESS XRS</option>        <option>BOMBARDIER GLOBAL 6500</option>        <option>BOMBARDIER GLOBAL 7500</option>        <option>BOMBARDIER GLOBAL 8000</option>        <option>BOMBARDIER LEARJET 70/75</option>        <option>CESSNA CARAVAN C208B+</option>        <option>CESSNA CITATION CJ3+</option>        <option>CESSNA CITATION CJ4</option>        <option>CESSNA CITATION CJ4 GEN2</option>        <option>CESSNA CITATION C650</option>        <option>CESSNA CITATION EXCEL</option>        <option>CESSNA CITATION EXCEL C560</option>        <option>CESSNA CITATION LONGITUDE</option>        <option>CESSNA CITATION MUSTANG</option>        <option>CESSNA CITATION LATITUDE</option>        <option>CESSNA CITATION SII</option>        <option>CESSNA CITATION X</option>        <option>CESSNA SOVEREIGN C680</option>        <option>CESSNA M2 GEN2</option>        <option>CIRRUS SF 50 VISION G2</option>        <option>DASSAULT 2000 LXS</option>        <option>DASSAULT FALCON 10X</option>        <option>DASSAULT FALCON 8X</option>        <option>ECLIPSE 500</option>        <option>EMBRAER 120ER</option>        <option>EMBRAER 145LR</option>        <option>EMBRAER PHENOM 100</option>        <option>EMBRAER PHENOM 100EV</option>        <option>EMBRAER PHENOM 300</option>        <option>EMBRAER PHENOM 300E</option>        <option>EMBRAER PRAETOR 500</option>        <option>EMBRAER PRAETOR 600</option>        <option>GULFSTREAM 550</option>        <option>GULFSTREAM 650ER</option>        <option>GULFSTREAM G150</option>        <option>GULFSTREAM G280</option>        <option>GULFSTREAM G450</option>        <option>GULFSTREAM G500</option>        <option>GULFSTREAM G600</option>        <option>GULFSTREAM G650</option>        <option>GULFSTREAM G700</option>        <option>GULFSTREAM G800</option>        <option>HAWKER 400</option>        <option>HAWKER 850XP</option>        <option>HAWKER 900XP</option>        <option>HONDA ELITE</option>        <option>NEXTANT 400XT</option>        <option>PIAGGIO P.180 AVANTI</option>        <option>PILATUS PC 24</option>        <option>PILATUS PC-12</option>      </select>      <select id="speedSelect" class="speed-select">        <option value="200">200 KTS</option>        <option value="225">225 KTS </option>        <option value="240">240 KTS </option>        <option value="300">300 KTS</option>        <option value="325">325 KTS </option>        <option value="350">350 KTS </option>        <option value="375">375 KTS </option>        <option value="400">400 KTS </option>        <option value="425">425 KTS </option>        <option value="450">450 KTS </option>        <option value="475">475 KTS </option>        <option value="500">500 KTS </option>      </select>    </div>    <div id="status">Loading data...</div>    <table>      <colgroup><col><col><col><col></colgroup>      <thead>        <tr>          <th>DEPARTURE AIRPORT</th>          <th>ARRIVAL AIRPORT</th>          <th>FLIGHT DISTANCE</th>          <th>FLIGHT TIME</th>        </tr>      </thead>      <tbody>        <tr>          <td>            <div class="autocomplete-wrapper">              <input type="text" id="departure1" autocomplete="off" />              <div class="autocomplete-list" id="list-departure1" style="display:none;"></div>            </div>          </td>          <td>            <div class="autocomplete-wrapper">              <input type="text" id="arrival1" autocomplete="off" />              <div class="autocomplete-list" id="list-arrival1" style="display:none;"></div>            </div>          </td>          <td><input type="text" id="distance1" readonly /></td>          <td><input type="text" id="time1" readonly /></td>        </tr>        <tr>          <td>            <div class="autocomplete-wrapper">              <input type="text" id="departure2" autocomplete="off" />              <div class="autocomplete-list" id="list-departure2" style="display:none;"></div>            </div>          </td>          <td>            <div class="autocomplete-wrapper">              <input type="text" id="arrival2" autocomplete="off" />              <div class="autocomplete-list" id="list-arrival2" style="display:none;"></div>            </div>          </td>          <td><input type="text" id="distance2" readonly /></td>          <td><input type="text" id="time2" readonly /></td>        </tr>        <tr>          <td>            <div class="autocomplete-wrapper">              <input type="text" id="departure3" autocomplete="off" />              <div class="autocomplete-list" id="list-departure3" style="display:none;"></div>            </div>          </td>          <td>            <div class="autocomplete-wrapper">              <input type="text" id="arrival3" autocomplete="off" />              <div class="autocomplete-list" id="list-arrival3" style="display:none;"></div>            </div>          </td>          <td><input type="text" id="distance3" readonly /></td>          <td><input type="text" id="time3" readonly /></td>        </tr>        <tr>          <td>            <div class="autocomplete-wrapper">              <input type="text" id="departure4" autocomplete="off" />              <div class="autocomplete-list" id="list-departure4" style="display:none;"></div>            </div>          </td>          <td>            <div class="autocomplete-wrapper">              <input type="text" id="arrival4" autocomplete="off" />              <div class="autocomplete-list" id="list-arrival4" style="display:none;"></div>            </div>          </td>          <td><input type="text" id="distance4" readonly /></td>          <td><input type="text" id="time4" readonly /></td>        </tr>      </tbody>    </table>    <div class="buttons">      <button onclick="plotRoute()">PLOT FLIGHT ROUTE</button>      <button onclick="printToPDF()">PRINT SCREEN TO PDF</button>      <button onclick="clearAll()">CLEAR FLIGHT DATA</button>    </div>    <div id="map"></div>    <div class="display:none;" id="loader" style="display:none;"></div>  </div>  <script>    let map;    let airports = []; // only airports with both ICAO & IATA    let flightPaths = [];    let markers = new Map();    // Utility: uppercase & trim    function norm(s){ return (s||'').toUpperCase().trim(); }    function showStatus(msg,isError=false){      const s = document.getElementById('status');      s.textContent = msg;      s.style.display = 'block';      s.style.backgroundColor = isError ? '#f8d7da' : '#fff3cd';      s.style.borderColor = isError ? '#f5c6cb' : '#ffeaa7';    }    function hideStatus(){ document.getElementById('status').style.display = 'none'; }    function initMap(){      map = new google.maps.Map(document.getElementById('map'), {        center:{lat:0,lng:0},        zoom:2,        mapTypeId:'roadmap'      });      showStatus('Map loaded. Loading airports...');      loadAirports().then(()=>{        hideStatus();        setupAutocompleteForAll();      }).catch(err=>{        showStatus('Error loading airports: '+err.message, true);        console.error(err);      });    }    async function loadAirports(){      const res = await fetch('https://raw.githubusercontent.com/jpatokal/openflights/master/data/airports.dat');      if(!res.ok) throw new Error('HTTP ' + res.status);      const text = await res.text();      const lines = text.split('\n').filter(l => l.trim());      airports = lines.map(line => {        const p = line.split(',');        if(p.length < 8) return null;        const name = (p[1]||'').replace(/"/g,'').toUpperCase();        const city = (p[2]||'').replace(/"/g,'').toUpperCase();        const country = (p[3]||'').replace(/"/g,'').toUpperCase();        const iata = (p[4]||'').replace(/"/g,'').toUpperCase();        const icao = (p[5]||'').replace(/"/g,'').toUpperCase();        const lat = parseFloat(p[6]);        const lng = parseFloat(p[7]);        return { name, city, country, iata, icao, lat, lng };      }).filter(a => a && a.iata && a.icao && !isNaN(a.lat) && !isNaN(a.lng));      // sort by ICAO      airports.sort((a,b)=> a.icao.localeCompare(b.icao));      console.log('Airports loaded (with both codes):', airports.length);    }    // AUTOCOMPLETE: independent per input    function setupAutocompleteForAll(){      const ids = ['departure1','arrival1','departure2','arrival2','departure3','arrival3','departure4','arrival4'];      ids.forEach(id => setupAutocomplete(id));      // clicking outside should close all lists      document.addEventListener('click', (e)=>{        if (!e.target.matches('input')) {          document.querySelectorAll('.autocomplete-list').forEach(el => el.style.display='none');        }      });    }    function setupAutocomplete(inputId){      const input = document.getElementById(inputId);      const list = document.getElementById('list-' + inputId);      if(!input || !list) return;      input.value = ''; // ensure blank on load      input.addEventListener('input', () => {        const q = norm(input.value);        if(!q){ list.style.display='none'; return; }        const matches = getMatches(q);        renderList(list, matches, input);      });      input.addEventListener('focus', () => {        const q = norm(input.value);        if(q){          const matches = getMatches(q);          renderList(list, matches, input);        }      });      // hide list after selection will be handled in click handler below      input.addEventListener('blur', () => {        // small delay to allow click handler on suggestion to run        setTimeout(()=> { if(list) list.style.display='none'; }, 150);      });    }    // returns up to 10 matches (airports already sorted by ICAO)    function getMatches(q){      const results = [];      for(let i=0;i<airports.length && results.length<10;i++){        const a = airports[i];        if (a.icao.includes(q) || a.iata.includes(q) || a.city.includes(q) || a.name.includes(q)) {          results.push(a);        }      }      return results;    }    function renderList(listEl, matches, inputEl){      listEl.innerHTML = '';      if(!matches || matches.length===0){ listEl.style.display='none'; return; }      matches.forEach(a => {        const div = document.createElement('div');        div.className = 'autocomplete-item';        div.textContent = `${a.icao}, ${a.iata}, ${a.city}`;        div.addEventListener('click', () => {          inputEl.value = `${a.icao}, ${a.iata}, ${a.city}`;          listEl.style.display = 'none'; // hide immediately after selection        });        listEl.appendChild(div);      });      listEl.style.display = 'block';    }    function findAirportFromInput(inputValue){      const v = norm(inputValue);      if(!v) return null;      const parts = v.split('/');      const cand = parts[0] ? parts[0].trim() : v;      let apt = airports.find(a=> a.icao === cand || a.iata === cand);      if(apt) return apt;      apt = airports.find(a => `${a.icao}, ${a.iata}, ${a.city}` === v);      if(apt) return apt;      return airports.find(a => a.city.includes(v) || a.name.includes(v) || a.icao.includes(v) || a.iata.includes(v));    }    function haversineDistance(lat1,lon1,lat2,lon2){      const R = 3440.065;      const dLat = (lat2 - lat1) * Math.PI/180;      const dLon = (lon2 - lon1) * Math.PI/180;      const a = Math.sin(dLat/2)**2 + Math.cos(lat1*Math.PI/180)*Math.cos(lat2*Math.PI/180)*Math.sin(dLon/2)**2;      const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));      return R * c;    }    function formatTime(hours){      const h = Math.floor(hours);      const m = Math.floor((hours - h) * 60);      return `${h}:${m.toString().padStart(2,'0')} HRS`;    }    function plotRoute(){      if(!airports.length){ showStatus('Airports not loaded yet.', true); return; }      document.getElementById('loader').style.display = 'block';      // clear previous      flightPaths.forEach(p => p.setMap(null));      flightPaths = [];      markers.forEach(m => m.setMap(null));      markers.clear();      const speed = parseInt(document.getElementById('speedSelect').value || '300', 10);      let totalDistance = 0, totalTime = 0;      const bounds = new google.maps.LatLngBounds();      // red dot symbol used for location dot, halved size      const redDotSymbol = {        path: google.maps.SymbolPath.CIRCLE,        fillColor: '#FF0000',        fillOpacity: 1,        strokeColor: '#FF0000',        strokeWeight: 1,        scale: 2.5      };      for(let i=1;i<=4;i++){        const depVal = document.getElementById(`departure${i}`).value.trim();        const arrVal = document.getElementById(`arrival${i}`).value.trim();        // clear displays first        document.getElementById(`distance${i}`).value = '';        document.getElementById(`time${i}`).value = '';        const dep = findAirportFromInput(depVal);        const arr = findAirportFromInput(arrVal);        if(!dep || !arr) continue;        const distance = haversineDistance(dep.lat,dep.lng,arr.lat,arr.lng);        const timeHRS = distance / (speed || 1);        totalDistance += distance;        totalTime += timeHRS;        // fill inputs with ICAO, IATA, CITY (UPPERCASE) Ñ ensures consistent format        document.getElementById(`departure${i}`).value = `${dep.city}, (${dep.icao}, ${dep.iata})`;        document.getElementById(`arrival${i}`).value   = `${arr.city}, (${arr.icao}, ${arr.iata})`;        document.getElementById(`distance${i}`).value = `${distance.toFixed(1)} NM`;        document.getElementById(`time${i}`).value = formatTime(timeHRS);        // --- create small red dot marker ---        const depMarker = new google.maps.Marker({          position:{lat:dep.lat,lng:dep.lng},          map,          title:`${dep.city}, (${dep.icao}, ${dep.iata})`,          icon: redDotSymbol        });        const arrMarker = new google.maps.Marker({          position:{lat:arr.lat,lng:arr.lng},          map,          title:`${arr.city}, (${arr.icao}, ${arr.iata})`,          icon: redDotSymbol        });        markers.set(`dep-dot-${i}`, depMarker);        markers.set(`arr-dot-${i}`, arrMarker);        // --- create a badge marker with a pointer on the left ---        const createBadgeIconUrl = (iata) => {          const w = 48; // total width in px          const h = 20; // height in px          const rx = 6; // corner radius for rectangle (not used in path)          const fontSize = 14;          const text = iata || '';          const svg = `<svg xmlns='http://www.w3.org/2000/svg' width='${w}' height='${h}' viewBox='0 0 ${w} ${h}'>            <path d='M12 0 L0 10 L12 20 H${w} L${w} 0 Z' fill='#FF0000'/> <!-- Solid red rectangle with left pointer -->            <text x='50%' y='50%' dominant-baseline='middle' text-anchor='middle' font-family='Arial, sans-serif' font-size='${fontSize}' fill='#FFFFFF' font-weight='700' dx='4'>${text}</text> <!-- Centered vertically, moved closer to pointer by reducing dx to 4px -->          </svg>`;          return 'data:image/svg+xml;charset=UTF-8,' + encodeURIComponent(svg);        };        const depBadgeIcon = {          url: createBadgeIconUrl(dep.iata),          scaledSize: new google.maps.Size(48, 20),          origin: new google.maps.Point(0, 0),          anchor: new google.maps.Point(0, 10) // Anchor at the base of the pointer        };        const arrBadgeIcon = {          url: createBadgeIconUrl(arr.iata),          scaledSize: new google.maps.Size(48, 20),          origin: new google.maps.Point(0, 0),          anchor: new google.maps.Point(0, 10)        };        const depBadgeMarker = new google.maps.Marker({          position:{lat:dep.lat,lng:dep.lng},          map,          icon: depBadgeIcon,          title: dep.iata        });        const arrBadgeMarker = new google.maps.Marker({          position:{lat:arr.lat,lng:arr.lng},          map,          icon: arrBadgeIcon,          title: arr.iata        });        markers.set(`dep-badge-${i}`, depBadgeMarker);        markers.set(`arr-badge-${i}`, arrBadgeMarker);        bounds.extend({lat:dep.lat,lng:dep.lng});        bounds.extend({lat:arr.lat,lng:arr.lng});        // polyline        const path = new google.maps.Polyline({          path: [{lat:dep.lat,lng:dep.lng},{lat:arr.lat,lng:arr.lng}],          geodesic: true, strokeColor:'#FF0000', strokeOpacity:1.0, strokeWeight:2, map        });        flightPaths.push(path);      }      if(flightPaths.length>0) map.fitBounds(bounds);      // update calculation result panel (kept but optional)      document.getElementById('calcResult') && (document.getElementById('calcResult').style.display = totalTime ? 'block' : 'none');      document.getElementById('totalTime1') && (document.getElementById('totalTime1').textContent = totalTime ? formatTime(totalTime) : '');      document.getElementById('totalTime2') && (document.getElementById('totalTime2').textContent = totalTime ? formatTime(totalTime * 1.05) : '');      document.getElementById('totalTime3') && (document.getElementById('totalTime3').textContent = totalTime ? formatTime(totalTime * 1.1) : '');      document.getElementById('estimatedPrice') && (document.getElementById('estimatedPrice').textContent = totalDistance ? `$${(totalDistance*5).toFixed(2)}` : '');      document.getElementById('loader').style.display = 'none';    }    function clearAll(){      for(let i=1;i<=4;i++){        document.getElementById(`departure${i}`).value = '';        document.getElementById(`arrival${i}`).value = '';        document.getElementById(`distance${i}`).value = '';        document.getElementById(`time${i}`).value = '';        const listDep = document.getElementById(`list-departure${i}`);        const listArr = document.getElementById(`list-arrival${i}`);        if(listDep) listDep.style.display='none';        if(listArr) listArr.style.display='none';      }      flightPaths.forEach(p => p.setMap(null));      flightPaths = [];      markers.forEach(m => {        try { m.setMap(null); } catch(e){/*ignore*/ }      });      markers.clear();      document.getElementById('calcResult') && (document.getElementById('calcResult').style.display = 'none');      if(map){ map.setCenter({lat:0,lng:0}); map.setZoom(2); }    }    function printToPDF(){ window.print(); }    // Google Maps auth diagnostics    window.gm_authFailure = function(){      showStatus('Google Maps authentication failed Ñ check API key and referrer restrictions.', true);      console.error('gm_authFailure');    };  </script>  <!-- Google Maps API with provided key; callback=initMap -->  <script async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCKu5QRTKpUBOpgkNEAGJJhVkofZ-G8Iyk&callback=initMap"></script></body></html>